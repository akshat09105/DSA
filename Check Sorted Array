int isSorted(int n, vector<int> a) {
    // Write your code here.
    for(int i=0;i<=a.size()-2;i++){
        if(a[i]<=a[i+1]){
        }
        else{
            return 0;
        }
    }
    return 1;
}
